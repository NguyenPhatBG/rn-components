import React, { Component } from 'reacct';
import { View, Text } from 'react-native';

export default class VisibleGoogleMap extends Component {
    state = {  }
    render() {
        return (
            <View />
        );
    }
}